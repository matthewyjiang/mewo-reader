# Server

Hosted library for Mewo. The Android app talks to this through `HostedLibraryStore`.

Not written yet. When it is:

- One HTTP API
- SQLite if you run it yourself
- Postgres when we host it
- Library metadata, likes, and reading place live here
- EPUB files can live here later; the phone still parses them

Local and hosted are different shelves. This process is the hosted one.
